export interface Template {
  id: number;
  name: string;
  thumbnail: string;
  category: string;
  description: string;
  lastEdited: string;
}

export interface Resume {
  id: string;
  name: string;
  thumbnail: string;
  category: string;
  description: string;
  lastEdited: string;
}